import { TituloCabeceraDirective } from './titulo-cabecera.directive';

describe('TituloCabeceraDirective', () => {
  it('should create an instance', () => {
    const directive = new TituloCabeceraDirective();
    expect(directive).toBeTruthy();
  });
});
